'use client'

import React from 'react'

/**
 * @deprecated The global Toast notification system in `@components/common/Toast`
 * now manages unified Cart and Wishlist notifications. This component is preserved
 * for backward compatibility.
 */
export const CartNotification: React.FC = () => {
  return null
}

export default CartNotification
